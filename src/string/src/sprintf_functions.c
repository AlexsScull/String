#include "../include/s21_string.h"

int s21_sprintf(char *str, const char *format, ...) {
  /*Задача:
  Форматировать данные согласно format и записать результат в str.

  Логика работы:
  Разбор строки формата (format):

      Обрабатывает обычные символы (копирует их в str).

      При встрече % начинает разбор спецификатора:

          Флаги (-, +, , #, 0).

          Ширина (минимальное количество выводимых символов).

          Точность (количество знаков после точки для чисел, максимальная длина
  для строк).

          Модификатор длины (h, l, L).

          Спецификатор (d, i, u, o, x, X, f, e, E, g, G, c, s, p, n, %).

      Обработка аргументов (va_list):

          В зависимости от спецификатора извлекает аргумент из ... и форматирует
  его.

      Запись результата в str:

      Записывает отформатированные данные в буфер str.

  Возвращает количество записанных символов (без учета \0).

  */
  //    va_list args;
  //    va_start(args, format);

  //    char *ptr = str;
  //    while (*format) {
  //        if (*format == '%') {
  //            format++;
  //            switch (*format) {
  //                case 'd': {
  //                    int num = va_arg(args, int);
  //                    // Упрощённая конвертация int в строку
  //                    sprintf(ptr, "%d", num);
  //                    ptr += strlen(ptr);
  //                    break;
  //                }
  //                case 's': {
  //                    char *s = va_arg(args, char*);
  //                    strcpy(ptr, s);
  //                    ptr += strlen(s);
  //                    break;
  //                }
  //                // Добавьте другие спецификаторы (%c, %f и т.д.)
  //                default:
  //                    *ptr++ = *format;
  //            }
  //        } else {
  //            *ptr++ = *format;
  //        }
  //        format++;
  //    }
  //    *ptr = '\0';

  //    va_end(args);
  //    return ptr - str;  // Количество записанных символов
}